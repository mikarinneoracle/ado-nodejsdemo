apiVersion: v1
kind: Service
metadata:
  name: nodejsdemo-service
  labels:
    app: nodejsdemo
spec:
  type: LoadBalancer
  ports:
    - port: 80
      targetPort: 3000
      protocol: TCP
      name: http
  selector:
    app: nodejsdemo
